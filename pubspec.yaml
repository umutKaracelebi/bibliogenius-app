name: app
description: "BiblioGenius flutter application."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.6.0-alpha.1+1
environment:
  sdk: ^3.10.1
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  sqflite: ^2.4.2
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.5+1
  go_router: ^17.0.0
  path_provider: ^2.1.5
  mobile_scanner: ^5.2.3
  network_info_plus: ^7.0.0
  qr_flutter: ^4.1.0
  http: ^1.2.0
  share_plus: ^10.0.0
  shared_preferences: ^2.2.2
  flutter_colorpicker: ^1.0.3
  file_picker: ^8.0.0
  fl_chart: ^1.1.1
  flutter_dotenv: ^5.1.0
  flutter_svg: ^2.2.3
  tutorial_coach_mark: ^1.2.9
  path: ^1.9.1
  url_launcher: ^6.3.2
  cached_network_image: ^3.4.1
  universal_html: ^2.3.0
  package_info_plus: ^9.0.0
  rust_lib_app:
    path: rust_builder
  flutter_rust_bridge: 2.11.1
  freezed_annotation: ^3.1.0
  bonsoir: ^6.0.1  # Native Bonjour/mDNS for local discovery
  
  # Audio module dependencies (decoupled, optional feature)
  just_audio: ^0.9.40           # Cross-platform audio player
  audio_session: ^0.1.21        # Handle audio focus
  connectivity_plus: ^6.1.0     # WiFi detection for streaming
  webview_flutter: ^4.13.0
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  network_image_mock: ^2.1.1
  http_mock_adapter: ^0.6.1
  flutter_launcher_icons: ^0.14.4
  integration_test:
    sdk: flutter
  freezed: ^3.2.3
  build_runner: ^2.10.4

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/avatars/
    - assets/genie_mascot.jpg
    - assets/images/badges/